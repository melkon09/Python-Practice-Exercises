from pickle import dump, dumps

class Data:
    def _init__(self, alist, adict, anint, aset, adata):
        self.alist = alist
        self.adict - adict
        self.adata = adata
        self.anint = anint
        self.aset = aset
        sel