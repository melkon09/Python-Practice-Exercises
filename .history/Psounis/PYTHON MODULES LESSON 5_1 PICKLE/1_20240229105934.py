from pickle import dump, dumps

class Data:
    def _init__(self, alist, adict, anint, aset, adata):
        self.alist = alist
        self.adict - adict
        self.anint = anint
        self.aset = aset
        self.adata = adata

l=[1,2,3]

print(dumps())